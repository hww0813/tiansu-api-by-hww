spring:
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}
      config:
        data-key: data
        prefix: config
        format: yaml
        watch:
          enabled: true
          delay: 10000
          wait-time: 30








